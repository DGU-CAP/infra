name: CI

on:
  push:
    branches-ignore:
      - main

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Lint check
        run: |
          echo "Running lint..."
          exit 1  # 의도적 실패 — Slack 알림 테스트용. 실제 lint 붙이면 이 줄 제거.
