apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ai
spec:
  template:
    spec:
      containers:
        - name: ai
          imagePullPolicy: Never
          envFrom:
            - secretRef:
                name: ai-secret
