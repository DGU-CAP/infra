# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfplan
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# 환경별 변수 파일 (민감 정보 포함 가능)
*.auto.tfvars
*.tfvars.json

# Bootstrap은 로컬 state 사용 (원격 backend 미사용)
# bootstrap/terraform.tfstate 는 *.tfstate 패턴으로 이미 제외됨

# 에디터 / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
